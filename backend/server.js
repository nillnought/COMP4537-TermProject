// server logic 
const http = require("http");
const port = 8000; //temp port

class Server {
    constructor(){
        this.configureServer();
    }

    configureServer(){
        const server = http.createServer((req, res) => {
            
        })
    }
}